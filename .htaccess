RewriteEngine On

# Jeśli plik lub folder istnieje, nie ruszaj
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Wszystko inne kieruj na index.html w root
RewriteRule . /index.html [L]
